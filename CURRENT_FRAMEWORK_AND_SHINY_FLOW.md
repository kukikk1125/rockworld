# RockWorld 当前软件框架与异色记录交互说明

## 1. 当前一级导航框架

当前软件仍保持 4 个一级导航入口：

- 首页
- 记录
- 图鉴
- 我的

这是当前版本的主框架，不额外新增一级入口。

---

## 2. 当前页面职责

### 首页 `/`

首页负责两件核心事情：

- 展示“已经产生过记录”的进行中任务
- 提供“选择抓取方案”的入口

首页支持两类方案入口：

- 定向果实法
- 3×3混抓法

首页不会因为点击方案就直接把它算作“进行中任务”。  
只有该任务里真的产生了污染 / 原色 / 异色 / 球耗等记录后，才会稳定出现在任务列表语义里。

---

### 记录 `/history`

记录页承载当前所有任务记录的列表浏览与进入。

当前职责：

- 查看已有任务
- 进入任务记录页继续操作
- 删除任务

---

### 图鉴 `/pets`

图鉴页是全量异色精灵总览。

当前职责：

- 展示所有精灵头像与名称
- 展示已拥有异色徽章
- 提供实时搜索
- 点击进入精灵详情页

---

### 我的 `/settings`

我的页当前定位为数据总览页，而不是复杂设置页。

当前职责：

- 总异色数量
- 已拥有种类数量
- 总球耗
- 最近获得异色列表

---

### 任务记录页 `/tasks/[id]`

这是当前最核心的业务页。

页面结构分为三块：

- 顶部任务摘要区
- 精灵记录卡片区
- 底部辅助区

当前版本里，异色记录与存档逻辑都围绕这里完成。

---

### 精灵详情页 `/spirits/[id]`

这是二级页面，不属于底部一级导航。

当前职责：

- 汇总该精灵在所有任务中的记录
- 展示污染 / 原色 / 异色总次数
- 按任务来源拆分显示
- 提供快捷记录入口
- 提供“重新记录”入口

---

## 3. 当前数据结构概览

当前主数据对象有两类：

- `Spirit`
- `Task`

### Spirit

用于表示全局精灵信息。

主要字段：

- `id`
- `name`
- `image`
- `tags`

### Task

用于表示一个方案任务。

主要字段：

- `id`
- `taskName`
- `planId`
- `planName`
- `mode`
- `fruitRecipe`
- `targetSpiritIds`
- `spiritRecords`
- `ballUsage`
- `lastAction`
- `actionHistory`

### TaskSpiritRecord

这是任务里真正绑定到某只精灵的记录对象。

主要字段：

- `spiritId`
- `pollutionCount`
- `normalCount`
- `shinyCount`
- `shinyObtainedAt`

这意味着当前版本已经不是“一个任务只绑定一只精灵”的旧结构，而是：

- 一个任务下可以有多只精灵卡片
- 每只精灵单独记录污染 / 原色 / 异色

---

## 4. 任务记录页当前交互模型

当前任务页采用“精灵卡片组”交互。

也就是：

- 当前方案下每只可记录精灵，直接展示为一张卡片
- 不使用“点精灵后弹出底部操作面板”的旧方式
- 高频操作直接放在卡片本体上

每张精灵卡片包含：

- 精灵头像
- 精灵名称
- 当前状态标记
- 80 格保底进度可视化区
- 污染数量
- 原色数量
- 异色数量
- 污染 `+1`
- 原色 `+1`
- 异色 `+1`

---

## 5. 异色记录的交互逻辑

这是当前最重要的交互链路。

### 5.1 正常异色记录

用户在任务记录页中，面对某一只精灵卡片时：

1. 点击该卡片上的 `异色 +1`
2. 页面弹出确认弹窗
3. 用户确认后，这只精灵被记录为“已获得异色”
4. 系统为该精灵写入：
   - `shinyCount = 1`
   - `shinyObtainedAt = 当前时间`
5. 这条动作会被写入任务的 `actionHistory`
6. 该精灵卡片从“当前任务页的可记录区”中隐藏
7. 后续这只精灵只在图鉴 / 精灵详情 / 数据汇总里展示

也就是说，当前版本的异色记录，不只是“数字 +1”，而是直接触发“进入存档状态”。

---

### 5.2 补记异色

当前任务页顶部保留了一个 `补记异色` 入口。

它用于处理两类场景：

- 当前方案外的异色精灵
- 预设列表里没有，但用户确实想记录的异色精灵

交互流程如下：

1. 用户点击 `补记异色`
2. 系统弹出精灵选择弹窗
3. 用户可以：
   - 从已有全局精灵中选择
   - 或直接输入新名称新增精灵
4. 若是新精灵：
   - 系统先写入全局 `Spirit` 数据
   - 以后该精灵可以在图鉴和其它任务里复用
5. 然后系统把这只精灵作为异色记录写入当前任务
6. 该精灵不会变成当前任务里的常驻完整卡片区成员
7. 它会直接作为“已存档异色”进入后续汇总体系

这就是当前版本“允许新增未预设精灵，但不污染主卡片区”的实现方式。

---

## 6. 存档的交互逻辑

当前版本里，“异色记录”与“存档”是绑定在一起的。

也就是说：

- 异色一旦确认
- 就视为已存档

### 6.1 当前存档语义

当前的“存档”不是单独的新页面操作，而是数据状态变化：

- 精灵从“当前任务可继续记录”状态
- 进入“已获得异色并归档”状态

这会直接带来几个表现：

1. 当前任务记录页不再继续展示这只精灵卡片
2. 图鉴页会把这只精灵标记为“已拥有”
3. 精灵详情页会显示该异色累计次数
4. 我的页会把它计入总异色 / 最近获得

所以，当前版本的“存档”其实是一个全局状态同步动作，而不是只在任务页里改一个数字。

---

### 6.2 存档后为什么会从任务页隐藏

这是当前交互里非常关键的一条规则：

- 任务页聚焦的是“高频录入”
- 已经出异色的精灵，不再是高频录入对象

因此当前版本采取：

- 已出异色的精灵卡片，从主记录区隐藏
- 避免任务页继续堆积低频信息
- 让页面持续保持“还能继续刷的精灵优先”

这也和当前文档要求中的“任务页不承担重汇总、不承担复杂分析”是一致的。

---

### 6.3 存档后如果还想继续记录怎么办

当前版本采用的是“重新记录”机制，而不是在原卡片上继续混记。

用户如果还想继续记录某只已经出异色的精灵：

1. 去图鉴页找到该精灵
2. 进入精灵详情页
3. 在精灵详情页中选择某个方案卡片
4. 点击 `重新记录`
5. 系统重新打开一个新的任务记录上下文，继续刷这只精灵

这样做的目的，是避免：

- 已存档记录和新一轮记录混在同一张旧卡里
- 导致保底进度、异色次数、当前轮次语义混乱

---

## 7. 保底进度的交互逻辑

当前任务卡片上的保底进度采用 80 格可视化栅格。

统计规则：

- 保底进度 = `污染数量 + 原色数量`

注意：

- 污染和原色严格分开记录
- 但在保底逻辑上它们共同累加
- 异色数量不计入保底进度

颜色表达：

- 前面的污染格：红色
- 后续的原色格：蓝色
- 未记录格：浅色

因此用户在卡片上一眼就能看出：

- 污染多少
- 原色多少
- 合计离 80 还差多少

---

## 8. 撤销交互逻辑

当前任务页底部辅助区保留了：

- 最近一次操作提示
- 撤销上一步

当前撤销规则是：

- 只撤销最近一次操作
- 不做多层复杂历史编辑面板

当前可被撤销的动作包括：

- 污染 +1
- 原色 +1
- 异色 +1
- 补记异色
- 球耗变更

这样做的目的，是满足高频记录下的即时修正，而不把任务页做成复杂后台。

---

## 9. 精灵详情页中的快捷记录逻辑

精灵详情页也支持快捷记录：

- 污染 +1
- 原色 +1
- 异色 +1

但这里有一个前提：

- 所有快捷记录必须归属于某个任务

交互规则如下：

### 情况 1：只有一个进行中任务

- 直接记到该任务

### 情况 2：有多个进行中任务

- 弹出任务选择弹窗
- 用户选择后再写入

### 情况 3：没有进行中任务

- 弹提示
- 引导用户先从首页创建任务

这保证了“所有记录都必须有任务归属”，不会产生脱离任务的脏数据。

---

## 10. 当前版本中“异色记录”和“存档”的关系总结

可以用一句话概括当前逻辑：

> 在任务记录页里，异色确认 = 立刻存档 = 从当前可记录区移出 = 进入图鉴与全局汇总体系。

当前系统不是把“异色”和“存档”拆成两次操作，而是合成一次强语义动作。

这样做的好处是：

- 用户操作路径短
- 记录语义清楚
- 存档状态明确
- 图鉴和总览能立刻同步

---

## 11. 当前核心文件索引

与当前框架最相关的文件如下：

- `src/app/page.tsx`
- `src/app/history/page.tsx`
- `src/app/pets/page.tsx`
- `src/app/settings/page.tsx`
- `src/app/tasks/[id]/page.tsx`
- `src/app/spirits/[id]/page.tsx`
- `src/components/plans/plan-record-client.tsx`
- `src/components/tasks/spirit-detail-client.tsx`
- `src/components/tasks/spirit-picker-dialog.tsx`
- `src/components/tasks/task-select-dialog.tsx`
- `src/lib/storage.ts`
- `src/lib/calculations.ts`
- `src/lib/task-factory.ts`
- `src/types/index.ts`

---

## 12. 一份简化 ASCII 说明

```text
RockWorld
├─ 首页
│  ├─ 进行中任务
│  └─ 方案选择
│     ├─ 定向果实法
│     └─ 3×3混抓法
├─ 记录
│  └─ 任务列表
├─ 图鉴
│  ├─ 全量异色精灵
│  ├─ 已拥有徽章
│  └─ 搜索
├─ 我的
│  └─ 数据总览
└─ 任务记录页
   ├─ 顶部任务摘要
   ├─ 精灵卡片区
   │  ├─ 污染 +1
   │  ├─ 原色 +1
   │  ├─ 异色 +1
   │  └─ 80格保底进度
   └─ 底部辅助区
      ├─ 最近操作
      └─ 撤销上一步
```

---

## 13. 当前版本最重要的设计原则

### 原则 1

任务页优先服务“高频记录”，不承担重分析。

### 原则 2

污染、原色严格分开记录，但共同组成保底进度。

### 原则 3

异色确认后立即存档，不再停留在当前任务卡片区。

### 原则 4

所有快捷记录都必须绑定任务归属。

### 原则 5

允许补记未预设精灵，但补记项不反向污染主记录卡片区。

