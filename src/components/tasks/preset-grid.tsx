import { PetPreset, Task } from "@/types";

export function PresetGrid({
  presets,
  activeTasks,
  onPick,
}: {
  presets: PetPreset[];
  activeTasks: Task[];
  onPick: (preset: PetPreset) => void;
}) {
  return null;
}
